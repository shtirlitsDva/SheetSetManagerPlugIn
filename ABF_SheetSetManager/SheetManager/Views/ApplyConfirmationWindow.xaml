<hc:Window x:Class="SheetSetManager.SheetManager.Views.ApplyConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:SheetSetManager.SheetManager.Views"
        mc:Ignorable="d"
        Title="Confirm Changes"
           Height="450" Width="800">
    
    <Window.Resources>
        <ResourceDictionary Source="Styles/Styles.xaml"/>
    </Window.Resources>

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Confirm Changes" FontSize="18" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,0,0,10"/>

            <TextBlock Text="{Binding SummaryText}" TextWrapping="Wrap" Margin="0,0,0,10"/>

            <ScrollViewer Height="200">
                <ListBox ItemsSource="{Binding ModifiedSheets}" BorderThickness="1">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding SheetNumber}" FontWeight="Bold"/>
                                <ItemsControl ItemsSource="{Binding Changes}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Cancel" Width="100" Margin="10" Click="Cancel_Click"/>
                <Button Content="Apply" Width="100" Margin="10" Click="Apply_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</hc:Window>
